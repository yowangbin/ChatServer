var React = require('react');

var LoginBox = React.createClass({
    render: function() {
        return (
            <div className="login_box">
                   <input type="text"/>
            </div>
        )
    }
});

module.exports = LoginBox;
